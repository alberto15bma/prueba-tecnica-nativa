import { StyleSheet } from "react-native";

const estilos = StyleSheet.create({
textNegrita: {
  fontWeight: "bold",
},
row: {
  display:"flex",
  flexDirection: "row",
}

});

export default estilos;